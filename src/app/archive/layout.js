export const metadata = {
  title: 'Session History',
  description: 'Review your French grammar practice history and track your progress over time.',
  robots: {
    index: false,
    follow: false,
  },
}

export default function ArchiveLayout({ children }) {
  return children
}
